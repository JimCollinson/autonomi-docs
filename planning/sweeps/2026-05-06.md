# Upstream sweep — 2026-05-06 (synthetic test)

Synthetic happy-path test of the sweep envelope. Refreshes the `verified_date:` line on `docs/cli/use-the-cli.md`'s verification block with no SHA change, leaving every other repo file untouched.

This file exists only to exercise `sweep-guard` and `sweep-sha-reachability` against a clean envelope. The branch and PR will be deleted after review.
